/**
 * FrequentlyAskedQuestions container component.
 */
import {connect} from 'react-redux'

import FrequentlyAskedQuestionsUI from './frequently-asked-questions.ui.component'

const mapStateToProps = (state, props) => ({
})

const mapDispatchToProps = (disptach) => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(FrequentlyAskedQuestionsUI)
